# 不提交 Python 运行环境。两份都不进 git：
#
#   .venv\   开发时自己用的虚拟环境。它依赖建它时的绝对路径（pyvenv.cfg 里
#            写着 home = C:\Python314），换台机器就废 —— 所以它从来不该被打进
#            发布包，见下面 python\ 那条。
#
#   python\  随发布包发出去的便携 Python，约 300 MB、9000 个文件，全是二进制。
#            它是面板 exe 和引擎真正用的解释器，路径全相对、解压即用。
#
# 两者都是几百 MB 的二进制，进了 git 之后每次改代码都会产生巨大的 diff，
# 别人 clone 也要等好几分钟。完整能跑的那一份（含 python\ 和 exe）打包成 zip
# 放在 GitHub Releases 里，想直接用的去那里下载；想自己搭环境看 requirements.txt。
#
# ★ 用 /python/ 而不是 python/：只挡仓库根目录下这一个，别误伤 src\ 里
#   万一以后出现的同名目录。
.venv/
venv/
/python/

# 面板 exe。2026-09-19 起不再跟踪（此前它在仓库里留了 5 个副本，占全历史 blob 的 92.7%）。
#
#   它是个构建产物：build_gui.bat 用 PyInstaller 打到 dist\（dist/ 本来就忽略），
#   以前是把 dist\ 里的那份拷到根目录再提交。而这一份**单独跑不起来** ——
#   README 写得清楚，exe 只是个面板，真正干活的是它去调 python\ 里的解释器跑
#   src\main_loop.py，而 python\ 有 300 MB，从来不在仓库里。
#
#   ⇒ 需要 exe 的两个地方都从本机构建取：发布包由 make_release.py 从根目录取
#     （所以本地开发时照旧把 dist\KARDS AUTO.exe 拷到根目录，只是别提交），
#     想双击就用的用户去 Releases 下载完整包。
#
#   ★ 还有个更实际的理由：仓库里那份 exe「对不对得上源码」全靠人记着
#     （改了 src\gui.py 就得重打）。它不进版本控制之后，这个约定由
#     build_gui.bat 和发布流程来保证，不会悄悄过期。
KARDS AUTO.exe

# 运行日志。每跑一次就变，内容也没什么共享价值。
# 保留目录本身（git 不追踪空目录，所以放了个 .gitkeep 占位）。
logs/*
!logs/.gitkeep

# 面板窗口的位置和大小，每台机器都不一样。
config/gui_window.json

# 面板的访问口令。手机端带着 ?t=<这串> 就能打开面板，传上去等于把钥匙挂网上。
# 现在这个目录里没有这个文件，但只要在本机启动过一次 GUI 就会生成。
# 每次启动都会重新生成，删掉不影响使用。
config/panel_token.txt

# Python 缓存和打包中间产物
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
*.spec

# 排查故障时的临时存帧目录
shots/
shots_hp_bogus/
hp_bogus/

# 编辑器
.vscode/
.idea/
Thumbs.db
desktop.ini
