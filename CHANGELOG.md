# 变更记录

版本号写在 `config/app_version.json`，以那个文件为准。每个版本对应的 tag 与发布包见
[Releases](https://github.com/yumehanab1/OCR-Kards-Auto/releases)。

从新到旧排列。

---

## v0.1.11 — 2026-09-24

主题：**增加排位模式选项**。

### 新增

- **排位模式开关**：面板默认关闭，开启前弹出检测与封号风险警告，确认后才会传入 `--ranked`。
- **排位按钮识别**：加入排位模式按钮模板；排位模式缺少模板时会安全停在选模式页面，不会误点休闲模式或开始按钮。
- **已选状态处理**：识别排位按钮已经选中的橙色文字状态，避免游戏记住上次模式时重复运行卡住。

## v0.1.10 — 2026-09-23

主题：**结算补点改用主页面左上入口**。

### 修复

- **补点位置**：改用主页面左上角进入卡牌选择页的「开始」按钮中心，避开卡组选择页右下角的「开始」按钮，减少结算时误进入对局的风险。

## v0.1.9 — 2026-09-23

主题：**结算流程按胜负识别启动补点**。

### 修复

- **对局结束补点流程**：识别并点击胜利/失败结果后，按每秒一次启动最多十次补点，覆盖等级提升、任务和任务奖励等结算尾页。
- **停止误触发**：识别到主菜单、匹配、选牌或新对局后立即停止剩余补点；未知画面本身不再触发十次点击，避免对手回合误点。

## v0.1.8 — 2026-09-22

主题：**指令卡会打了** —— 674 张指令/反制里,「拖到中线以下就打出」的 286 张 +
「拖到目标卡/总部身上」的 180 张,一共 **466 个卡名**真的会打出去了;
顺手拆掉两个「卡住不动」的老问题(结算页死锁、手牌张数读不准)。

### 新增

- **指令卡支持**(`src/orders.py` + `config/order_plays.json`)。指令卡以前是**一律跳过**的
  —— 有些要你选目标,而「选哪个」当时没有判据。2026-09-20 用户把 674 张逐张给了打法,
  落成 `config/order_plays.json`:`direct` 286(拖到中线以下)、`target` 211(拖到目标身上)、
  `choice` 27(抉择)、`blacklist` 82(别带这张),其余是反制等暂不做。
  这一版放行 **direct + target 的 kind 1~4/6 = 466 个卡名**;kind 5「选择一张手牌」10 张
  归黑名单;★ **抉择(27)与三选一(20)这一版不做** —— 它们要「打出后弹选项」的坐标,
  得先拿真实弹窗帧标定,所以**「扫到了不打」是预期行为,不是故障**。
  判据只允许有一处来源:能不能打一律问 `orders.playable()`(扫描器 / 回合引擎 / 启动日志
  三处都问它 —— 这个项目在「counter / countermeasure」两套词汇上栽过一次)。
- **`target` 指令:拖到那张卡/总部身上**(用户确认的手势)。目标码 `1` 敌方单位(打不了总部)/
  `2` 敌方单位或总部 / `3` 敌方总部 / `4` 我方单位 / `6` 随意阵营单位(**一律指敌方**,
  用户 2026-09-21 决定);行码 `a` 仅前线敌方 / `b` 仅前线友方 / `c` 仅敌方支援线 /
  `d` 仅我方支援线。前线归属用现成的 `frontline_line.read_frontline_owner()`,
  **读不出就不打**;我方单位**只从 `our_support` 取**(前线那一行的归属是争夺的)。
- `docs/generated/指令卡清单.md`:由 `dev/order_list_doc.py` **生成**的清单(当前能打 / 黑名单 /
  暂不支持,每张带费用与规格出处)。改了判据重跑一次即可,不会和代码说法不一致。

### 修复

- **结算页「卡在里面出不来、还不会自己停」。** `tick_dismiss` 里画面一直判成 victory/defeat 时
  **只睡不点、也不增重试计数** ⇒ 12 次上限永不触发;而主循环在 dismiss 期间走 `continue`,
  **又绕过** `max_rounds` 的退出检查。触发器很讽刺:那两个模板其实是「胜利/失败」**横幅文字**
  (不是按钮),点上去没反应 → 状态还是 victory → 永久等下去。现在点完等 1.2s 还没切走,
  就当成「那一下没生效」,落进带 12 次上限的轮换点击。
- **结算页候选点改成四个角**(左上 → 右上 → 左下 → 右下)。用户实测:**除了点中心点哪都能跳过**;
  旧那套四个点全挤在中右部,一个失效就四个一起失效。顺带修掉一个 off-by-one:
  `tick_dismiss` 是先加计数再取点,老写法让「首选点」要第 5 次才轮到、日志还写成「#2」。
- **手牌张数读不准。** 根因不是探针间距,而是**张数取自「选中条目的 count」,而条目是在两个
  都不可靠的边缘测量之间挑的** —— 实机一局里引擎对「几张」给过 5/7/8 三个答案,而表里
  7/8/9 三条条目的边缘几乎一样(容差内都能过闸,命中率也分不出来)。现在:两级闸的候选
  **取并集**、探针**取并集 + 两端按需补探**、整条走完时按**实读**修正张数,并把
  「实走几根探针」写进日志。★ 已知限制:惰性扫描「提前收手」的回合不改张数(没走完不许猜)。
- **打完一局后的补点窗口**:每秒一下、共十下(用户提的)。★ 两道闸保证**非结算画面不会乱点**
  (两个入口都要求「这一局已经结束过」)—— 旧版只按「画面认不出 ≥10 秒」触发,实测
  **对手回合也开**,一局白点 20 下。**已知限制:这个点的坐标是过时值**(实际落在空白处),
  现在靠连点密度生效,待重新标定。
- 点击失败时引擎不再崩(`actions_mod` 的 `NameError`)。
- `--max-rounds` 的帮助文本:它是**按整局算**(出现胜/负结算才 +1),不是回合数 ——
  另一台机器把它读成「跑 1 轮」、跑了 5 个回合没停就记成了疑点,行为本来就是对的。

### 内部

- **并集探针去重门槛 15 → 35**:扫描慢的真凶(同一张牌被拆成两根针读两遍)。实机验收
  重复身份 14 根 → **0 个**、探针 17 → 10 根,27 秒那种扫描没再出现。
  ★ **这是止血,不是干净修法**:离线已证明「同一张牌的针距偏移(最大 131px)比不同牌的
  最小间距(46px)还大」,两个区间重叠 ⇒ 不存在任何固定门槛能同时做对。真正的修法是
  **按牌的身份对齐**(用每条布局的 `left_edge` 归一),留给下一轮。判据出处
  `dev/probe_dedup_eval.py`,动门槛前先跑它。
- `dev/_fix_window.py`:**起跑前先跑** —— 窗口被弄到屏幕外或压扁时**所有点击必然失败**
  (算出来的屏幕坐标是负的,`SetCursorPos` 放不过去),这个脚本一键摆回 `(40,30)` +
  客户区 1280x720。
- 面板「只看不动(dry-run)」文案改成 **「只看不动(调试用)」**,并写明**平时不需要开**。
- `config/card_hashes.json`:收下实机学到的卡图指纹(81 条 / 25 个卡名)。

### 验收

- **实机 14 局**(2026-09-21,报告 `docs/第二十四轮实机报告.md`):打完整局并自己停 3 次
  (胜 2 负 1,均 rc=0);四局合计 `指令打出` **21** / `指令没打出去` **3** / `playing` 25 /
  `target 决策行` 8 / `[order_target] 不打` **0** / 被遮挡 0。`[dismiss]` 那条路单独验过
  (从失败界面起跑,一次就过)。
- **离线 11 套用例全绿**(含 `turn_logic_test` 全链路)。★ 用例要在**有 `shots/` 的那棵树**上跑
  —— `shots/` 不进 git,在仓库树上跑会得到「找不到真值帧」的假红。

---

## v0.1.7 — 2026-09-20

主题：**两个「到点就不动了」** —— 面板日志滚不动、回合打完了不结束回合。

### 修复

- **回合打完了却不结束回合。** 用户报「有时候操作完了不结束回合」，日志里的表现是机器人
  每 1.5 秒存一帧、就是不点结束回合。根因是 `hand_scanner_v2.py` 的 `find_playable()`
  里少了一个守卫：布局表一条都对不上时（`cands` 为空）`cands[0]` 直接 `IndexError`，
  而紧跟在后面那段「布局表对不上 → 盲扫兜底」的代码**永远走不到**。异常冒到
  `main_loop` 的 `except`，这一 tick 就**没执行到「该不该结束回合」**，下一 tick 从同一步重来。
  手牌出完那一刻最容易踩（布局表里没有「0 张」这条）。
  实测一份 12h44m / 342 回合的日志：**34 段卡死、每段 10~61 秒**，其中 22 段紧跟
  「记忆剩 0 张」。已修，并补上离线用例 `dev/find_playable_test.py`（这条路此前一个用例都没有）。
- **面板日志「滚到一定时候就不动了」。** 面板的日志尾巴只取 400 行，所以日志一旦超过 400 行，
  `log_lines.length` 就**永远是 400** —— 而前端判断「要不要重画」用的正是行数，从那以后恒为假，
  日志区再也不更新（往下翻也确实没内容，那 400 行是冻结的）。
  现在改成用「文件大小 + 纳秒级 mtime」的指纹 `log_rev` 判变化，追加一行就一定变，不会饱和。
  顺手补掉一个**沉默失败**：面板读不到状态时（原来是个空 `catch`）会把日志区标题改成
  「⚠ 面板读不到状态」并变红，不再装成「日志本来就没新内容」。

### 新增

- **「不抢鼠标」的窗口消息输入**（`src/actions.py` 的 `INPUT_MODE`，可选 `"message"`，
  **默认仍是 `"physical"`**）。原理是不碰系统光标、直接往窗口发消息；还在实机实测阶段，
  默认不启用。
- `dev/click_probe.py`：实测探针（含 `--hover`，只发移动消息），用来判断「扫描手牌能不能也不碰光标」。
- `dev/find_playable_test.py`：惰性扫描兜底路的离线用例（布局对不上必须走兜底且不许抛异常 /
  兜底返回的牌要交回引擎 / 对照组：容差内不许走兜底）。

### 内部

- 卡库目录 `card_db_test/` → `card_db/`（旧名字保留作兜底），`make_release.py` 自检同步跟着改。
- `make_release.py`：**跳过 `*.bak` 这类备份文件** —— 本机的 `KARDS AUTO.exe.bak`（上一版面板）
  以前会被原样打进发布包。
- 仓库整理：README 重写，补 `CONTRIBUTING.md` / `CHANGELOG.md` / issue 与 PR 模板，
  `KARDS AUTO.exe` 不再进仓库（**发布包里照旧带**）。

### 说明

- 面板 exe 已重打（改了 `src\gui.py` 的日志滚动判据）。**验证新面板在不在 exe 里的办法**：
  解压后跑 `KARDS AUTO.exe --selftest`，输出里出现 `"log_rev"` 字段就说明是新的。

---

## v0.1.6 — 2026-09-19

主题：**鼠标动作失败不再把整局带走**，以及「关掉面板没带走引擎」这个真 bug。

### 修复

- **移动鼠标失败会导致整个引擎退出。** 用户报「运行到卡组页面不会点确定」，实际不是不会点，
  而是 `win32api.SetCursorPos` 抛了异常、当时没人接，异常一路冒到顶层，程序直接结束 ——
  界面上的表现就是「停在那儿不动」。现在输入动作全部软着陆：
  - `set_cursor` / `move_stepped` / `click` / `click_center_of` / `move_drag` 一律返回布尔值，不抛异常；
  - 挪不过去时**绝不按下**（光标没到位就按下，点的是光标当时所在的位置，可能落到别的窗口的按钮上）；
  - 拖拽中途挪不动会**松手**再报失败，不会让鼠标一直按着；
  - 连续失败 3 次会点名原因：游戏 / Steam 以管理员身份运行时，面板也必须用管理员身份。
- **关掉面板不会带走引擎子进程。** 引擎是面板起的 `subprocess`，以前关窗口时它还活着并占着
  单实例锁，于是下次点「开始」直接被拒 —— 一位用户的日志里「拒绝启动第二个实例」出现了 48 次，
  只能去任务管理器手动杀进程。现在关面板会把引擎一起停掉；万一还是被拒，日志直接写出
  **占用者 PID** 和可照抄的 `taskkill /PID <pid> /F`。
- **结算页点不动。** 点击位置从「屏幕正中附近轮换」改成「左起 3/4 + 竖直居中」
  （1280x720 下是 `(960,360)`），并且不再点屏幕正中；备选点同样不居中。

### 新增

- **引擎内部报错第一次带完整 traceback**（同一种错误只报一次，后面只报次数）。
  在这之前，`turn_engine` 的异常只打一行 `类型: 消息`，日志里有一个 `IndexError` 因此无法定位。

### 说明

- 面板 exe 重打（改了 `src\gui.py`）。
- `make_release.py` 自检清单新增 `card_db_test/kards_data.json` 与 `config/kredits_digits/`。

---

## v0.1.5 — 2026-09-19

主题：**「明明没费用，还去拖一张出不起的牌被游戏拒绝」= 发布包里又少了一个数据文件。**

### 修复

- 读费用数字要用的 **10 个数字模板**原先放在 `shots/kredits/samples_r1/`，而 `shots/` 是排查用的
  临时目录、不进发布包 —— 于是**每个发布包都载入 0 个模板**，费用一次都没真读到过（日志里
  全是「上一回合 +1」推算），攻击和前进花的钱没人记账，账本比真实费用高，拖出去就被拒绝。
  - 模板搬到 `config/kredits_digits/`（`config/` 随包发；总部血量的数字模板早就是这个约定）；
  - `kredits_templates.status()` 一行说清「10/10 个数字」还是「一个都没载入」，引擎开跑第一行就打；
  - `make_release.py` 自检加上这个文件。

### 验收

- 离线判据：60 张实机存帧喂给读数器，修前 **0/60** 读出来，修后 **48/60**
  （剩下 12 张根本不是对局画面）。人眼核对：徽章上是 `2`，读出来就是 `2`。
- 新增动作：**打完包解压到空目录再验一次**（卡库张数 / 费用模板数 / 版本号）。

---

## v0.1.4 — 2026-09-19

### 新增

- **面板一开起来，后台自己先查一次更新**。查到新版本就把右上角按钮改成「有新版本 vX」，
  没查到就什么都不说。每次查完都往 `logs\update_check.log` 追加一行（含开机那次），
  这样「没有新版本」和「后台任务根本没跑」能分得开。

### 说明

- 自动这条路绝不影响面板：查询出错、改按钮失败都只记一行日志，不弹错。

---

## v0.1.3 — 2026-09-19

主题：**「检测更新」真的做出来了**（以前点了只会说「更新源还没配置」）。

### 新增

- `src/update_check.py`：读 GitHub 发布页并与本地版本号比较。
  - 版本号按**数字**比较：按字符串比会得出 `0.1.10 < 0.1.9`，第 10 个补丁的提示就永远不会出现；
  - 有新版本就说清版本号与包大小，并把按钮变成「打开发布页」；
  - **查不到就说查不到**：无网络 / 超时 / GitHub 限流 / 代理拦截页 / 仓库没发过 Release，
    每种都有各自的话，绝不把它说成「已是最新」；
  - 未登录的 GitHub 接口每小时只有 60 次调用（按 IP 算），被限流时自动退到不限流的
    `releases.atom`，代价是那条路上拿不到包大小。

### 修复

- `read_version()` 改用 `utf-8-sig` 读取：带 BOM 的 `app_version.json` 会让 `json.load` 抛异常，
  而那个异常被吞掉后版本号会**静默变成 `0.0.0`** —— 在「检测更新」上线后，这意味着永远提示有新版本。

---

## v0.1.2 — 2026-09-19

主题：**「手里有能出的牌，却整回合不出」这一族毛病。** 实机跑局 + 日志与截图逐帧对照查出四处。

### 修复

- **发布包里漏了卡库**（`card_db_test\kards_data.json`）。文件不在包里时引擎**不报错**，
  只是「卡名 → 费用」整条路悄悄失效，费用只剩一条不稳的兜底路，于是好几个回合一张牌都出不去。
  现在文件随包发，引擎开局第一行会报「卡库：1558 张卡名」还是「卡库没载入」。
- **卡名匹配的「精确」那一级一直没生效。** 库里有 659 张卡名带空格（如「第 5 步兵旅」），
  而程序拿去掉空格的读法去比带空格的原名，永远不相等 → 掉进模糊匹配 → 被一张更长、恰好包含
  它的卡名抢走（实测「第 5 步兵旅」认成「维尔纽斯第 5 步兵旅」，2 费认成 6 费 → 判「出不起」→
  那张牌整局都不出）。现在库里 1558 张名字都能认出自己。
- **卡名只差一个字就整张认不出。** OCR 常把「喷火 Mk Ia」读成「喷火 Mkla」（字母 I 读成 l）、
  把「燧」读成「燃」。新增「近似命中」这一级（编辑距离），但要求最佳明显优于次佳，
  分不清就宁可不认 —— 认错比认不出更坏，费用会跟着错。
- **手牌记忆把「是单位、但费用没读出来」当成「出不起」，以后每回合都不再看它一眼**，
  一张牌只要有一次没读准就整局再也出不去。现在这类牌每回合都会重新看一眼。
- 手牌扇形的左右边缘检测换成「锚点 + 向外延伸」的迟滞判据，最外那张牌只露出一条窄缝时也能量准
  （以前会把 7 张手牌量成 4 张，那一回合白扫）。

### 修复（发布流程）

- `make_release.py` 的 `SKIP_DIRS` 补上 `logs`。此前 v0.1.1 的包里带上了
  `logs\main_loop.log`（含对手昵称）和 `logs\gui_run.log`（含本机绝对路径）。

### 已知问题

- 手牌 **8 张和 9 张**靠外边缘分不开（两者几乎重合），偶尔会少数一张，那一回合可能少出一次牌。
  方向已定：改成直接数牌，不再量边缘。

---

## v0.1.1 — 2026-09-18

主题：**修掉「点开始就退 103」**。

### 修复

- v0.1.0 的包里混进了开发机上的 `.venv`，而 Python 虚拟环境记着建它那台机器的绝对路径
  （`pyvenv.cfg` 里的 `home = ...`），**换台机器就不能用** —— 于是每个下载的人点「开始」
  都立刻以退出码 103 结束，日志里一个字都没有。现在换成随包携带的便携 Python。
- 面板启动引擎前会**真的试跑一次解释器**，不对就直接说清哪里不对、该怎么办，
  而不是甩一个裸退出码。
- 解压到**带中文的目录**也能跑了（v0.1.0 在中文路径下读不出任何模板图，引擎会安静地什么都认不出来）。
- `install.cmd` 遇到坏的 `.venv` 会自己删掉重建，不再「已经有一个了，直接用」。

---

## v0.1.0 — 2026-09-18

首个发布版本：整局流程（主界面 → 选牌组 → 排队 → 换牌 → 对局 → 结算 → 下一局）、
手牌识别、费用读取、部署、攻击、上前线与面板 GUI。

已知问题：见上面 v0.1.1 的两条（`.venv` 不可移植、中文路径失效）。
